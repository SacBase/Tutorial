use Array: all;
use SimplePrint: all;

int main()
{
  a = [1,2,3,4];

  b = modarray( a, [0], 9);
  c = toi( print( a));
  a = b;
  c = toi( print( a));

  a = modarray( a, [1], 8);
  c = toi( print(a));

  return(0);
}

