use Array: all;
use SimplePrint: all;

int main()
{
  a = [1,2,3,4];

  b = modarray( a, [0], 9);
  c = print( a);
  a = b;
  c+= print( a);

  a = modarray( a, [1], 8);
  c+= print(a);

  return(c);
}

