use Array: all;
use SimplePrint: all;

int main()
{
  a = [1,2,3,4];
  b = [a,a];

  a = modarray( modarray( a, [0], 0), [1], 0);
  d = print( a);
  d = print( b);
  b = modarray( b, [1], a);
  c = print(b);

  return(c);
}

