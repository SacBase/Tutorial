use Array: all;
use SimplePrint: all;

int main()
{
  a = toi( print( reshape( [5], [1,2,3,4,5])));
  a = toi( print( reshape( [3,2], [1,2,3,4,5,6])));
  a = toi( print( reshape( [3,2,1], [1,2,3,4,5,6])));
  return(0);
}

