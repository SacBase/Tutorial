import StdIO: all;

#include "NTCtemplates.mac"

int main()
{
  /* 
   * the compiler will reject this program, as the product
   * of the shape does not match the length of the elements!
   */
  a = reshape( [2], [1,2,3]);
  print( a);

  return(0);
}

