import StdIO: all;

#include "NTCtemplates.mac"

int main()
{
  vect = [1,2,3,4,5,6,7,8,9];

  mat = [ vect, vect+10, vect+20];
  print( mat);

  print( mat[[1]] );
  print( mat[[1,.]] );
  print( mat[[1,...]] );

  print( mat[[.,1]] );
  print( mat[[...,1]] );

  print( mat[[1,...,1]] );

  arr3d = [ mat, mat];
  print( arr3d);

  print( arr3d[[.,1]] );
  print( arr3d[[...,1]] );

  return(0);
}

