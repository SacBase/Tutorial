use Array: all;
use Math: all;
use SimplePrint: all;

int main()
{
  vect = [1,2,3,4,5,6,7,8,9];

  mat = [ vect, vect+10, vect+20];
  a = print( mat);

  mat2 = where( (mat % 2) == 0, mat, -mat);
  a+= print(mat2);

  a+= print( max( mat2, 0));

  return(a);
}

