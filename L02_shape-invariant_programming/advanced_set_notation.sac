use StdIO: all;
use Array: all;

int main()
{
  vect = [0,1,2,3];

  mat = { [.,i] -> vect[[i]]*4 + vect };
  print( mat);

  arr3d = { [i] -> vect[[i]]*16 + mat};
  print( arr3d);

  arr3d = { [.,.,i] -> vect[[i]]*16 + mat};
  print( arr3d);

  arr3d = { [.,i] -> vect[[i]]*16 + mat};
  print( arr3d);

  return(0);
}

