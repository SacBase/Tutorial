use Array: all;
use StdIO: all;

int main()
{
  a = [1,2,3,4];
  b = [a,a];

  a = modarray( modarray( a, [0], 0), [1], 0);
  print(a);
  print(b);
  b = modarray( b, [1], a);
  print(b);

  return(0);
}

